import { PasswordConfirmValidationDirective } from './password-confirm-validation.directive';

describe('PasswordConfirmValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordConfirmValidationDirective();
    expect(directive).toBeTruthy();
  });
});
