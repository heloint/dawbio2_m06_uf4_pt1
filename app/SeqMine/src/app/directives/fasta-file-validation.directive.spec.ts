import { FastaFileValidationDirective } from './fasta-file-validation.directive';

describe('FastaFileValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new FastaFileValidationDirective();
    expect(directive).toBeTruthy();
  });
});
